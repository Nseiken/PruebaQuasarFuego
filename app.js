// const QuasarController = require('./controllers/QuasarController');
// Establecer el MVC con las rutas
// Establecer los servicios con los métodos getLocation y getMessage
// Separar distancias de los vectores del satélite
const main = async() => {
    // let satellites = new QuasarController().createSatellites();
    // let positionMessage = new QuasarController().findPositionSatellites();
    // console.log(positionMessage);
    // let finalMessage =  new QuasarController().getMessagesFromFile();
    // console.log(finalMessage);
}

main();